/**
 * Data classes that store the visual layout of mathematical expressions, but do not perform actual rendering.  These
 * structures are used for layout calculations and hit testing.  The sizes and locations of boxes associated with
 * expression components are set by a rendering and layout engine as they are determined.
 */
package dev.mathops.math.expr.layout;