/**
 * Support for rendering expressions.
 *
 * <p>
 * Rendering-related data structures are attached to an expression data structure to allow the expression to be rendered
 * to screen or print.  Each rendering structure points to an underlying expression structure which can be used to find
 * the rendering structures of other parts of the expression.
 */
package dev.mathops.math.expr.render;